# Shell/Python/JS scripts and VHS tapes must stay LF — they run in Linux bash
# (Docker VHS) and via node; CRLF breaks them ("$'\r': command not found").
*.sh   text eol=lf
*.py   text eol=lf
*.mjs  text eol=lf
*.tape text eol=lf
*.html text eol=lf
*.yaml text eol=lf
*.json text eol=lf
